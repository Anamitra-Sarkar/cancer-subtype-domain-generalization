# Cancer Subtype Domain-Generalized Classification

Domain-generalized learning for cancer molecular subtype classification: a
classifier trained on data from one cohort/platform that generalizes to
held-out cohorts/platforms it never saw during training (real
domain-generalization methodology, not just standard cross-validation).
